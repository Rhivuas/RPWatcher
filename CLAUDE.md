# RPWatcher – Claude Code Project Instructions

@AGENTS.md

## Current project state

- The current public stable release is RPWatcher 1.0.0.
- The published tag `v1.0.0` is immutable.
- The release commit is `9e18d16db86ef42b19699ce5aab2f3021bad9d12`.
- New fixes and improvements require new commits and a new version.
- Never rewrite published history or move published tags.
- Work only on the explicitly assigned phase or report.
- Do not implement adjacent suggestions without approval.
- The user performs all in-game acceptance tests.
- Every implementation task must end with a structured report and manual test instructions.
- Do not publish, tag, upload, or create a release unless explicitly instructed.

## Tool transition

- Claude Code is the active implementation tool.
- `AGENTS.md` remains the authoritative detailed project rule set.
- Keep `CLAUDE.md` concise and avoid duplicating the full contents of `AGENTS.md`.
